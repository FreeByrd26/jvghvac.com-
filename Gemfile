source "https://rubygems.org"

# GitHub Pages — pin to the version GitHub Pages uses
gem "github-pages", group: :jekyll_plugins

group :jekyll_plugins do
  gem "jekyll-sitemap"
  gem "jekyll-seo-tag"
end
